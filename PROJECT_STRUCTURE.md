# 🎓 AI 기반 프로젝트 평가 시스템 - 프로젝트 구조

## 📁 디렉토리 레이아웃

```
260214-ai-evaluate/
├── 📂 backend/                          # Express.js 백엔드
│   ├── server.js                        # 메인 서버 진입점
│   ├── package.json                     # Node.js 의존성
│   ├── .env                             # 환경 변수 (API 키 등)
│   ├── .gitignore
│   │
│   ├── 📂 db/
│   │   └── database.js                  # SQLite 데이터베이스 관리
│   │
│   ├── 📂 services/
│   │   └── evaluationEngine.js          # Claude API 평가 엔진
│   │
│   └── 📂 data/
│       └── rubrics.js                   # 평가 루브릭 정의 (JSON)
│
├── 📂 frontend/                         # React 프론트엔드
│   ├── package.json                     # React 의존성
│   ├── .gitignore
│   │
│   ├── 📂 public/
│   │   └── index.html                   # HTML 진입점
│   │
│   └── 📂 src/
│       ├── index.js                     # React 진입점
│       ├── index.css                    # 기본 스타일
│       ├── App.js                       # 메인 앱 컴포넌트
│       ├── App.css                      # 전역 스타일
│       │
│       ├── 📂 pages/                    # 페이지 컴포넌트
│       │   ├── Dashboard.js             # 🏠 홈 대시보드
│       │   ├── StudentManagement.js     # 👥 학생 관리
│       │   ├── ArtifactInput.js         # 📝 산출물 입력
│       │   ├── IndividualEvaluation.js  # ⭐ 개별 평가
│       │   ├── GrowthAnalysis.js        # 📈 성장 분석
│       │   ├── Portfolio.js             # 🎯 포트폴리오
│       │   └── ClassStatus.js           # 📊 학급 현황
│       │
│       └── 📂 components/               # 재사용 컴포넌트 (확장용)
│
├── 📄 README.md                         # 📖 프로젝트 전체 설명
├── 📄 SETUP.md                          # 🚀 설치 및 실행 가이드
├── 📄 PROJECT_STRUCTURE.md              # 📁 이 파일
├── 📄 .gitignore                        # Git 무시 규칙
│
├── 🚀 start.sh                          # macOS/Linux 실행 스크립트
└── 🚀 start.bat                         # Windows 실행 스크립트

📋 원본 문서
├── ai_assessment_system_guide_v2.md    # 교육 철학 & 루브릭 상세
└── tech_spec_for_developers.md          # 기술 스펙 & 아키텍처
```

## 🗄️ 데이터베이스 구조

```
backend/data/assessment.db
├── students
│   ├── id (PK)
│   ├── name (TEXT)
│   ├── class_name (TEXT)
│   ├── number (INTEGER)
│   └── created_at (DATETIME)
│
├── artifacts
│   ├── id (PK)
│   ├── student_id (FK)
│   ├── practice_type (TEXT)
│   ├── raw_text (TEXT)
│   ├── structured_data (JSON)
│   ├── date (DATE)
│   ├── session (TEXT)
│   └── created_at (DATETIME)
│
├── evaluations
│   ├── id (PK)
│   ├── artifact_id (FK)
│   ├── eval_type (TEXT)
│   ├── scores (JSON)
│   ├── total_score (REAL)
│   ├── feedback (TEXT)
│   └── created_at (DATETIME)
│
└── growth_analyses
    ├── id (PK)
    ├── student_id (FK)
    ├── practice_type (TEXT)
    ├── artifact_ids (JSON)
    ├── analysis (TEXT)
    └── created_at (DATETIME)
```

## 🔌 API 엔드포인트

### 학생 관리
- `GET /api/students` - 모든 학생 조회
- `POST /api/students` - 학생 추가
- `GET /api/students/:id` - 특정 학생 조회
- `DELETE /api/students/:id` - 학생 삭제

### 산출물 관리
- `GET /api/artifacts` - 산출물 조회
- `POST /api/artifacts` - 산출물 추가
- `GET /api/artifacts/:id` - 특정 산출물 조회
- `DELETE /api/artifacts/:id` - 산출물 삭제

### 평가
- `GET /api/evaluations` - 평가 결과 조회
- `POST /api/evaluate` - AI 평가 수행

### 분석
- `POST /api/growth-analysis` - 성장 분석
- `POST /api/portfolio-feedback` - 포트폴리오 피드백

## 🎯 주요 기술 스택

### 백엔드
| 기술 | 용도 |
|------|------|
| Node.js | 런타임 |
| Express.js | 웹 프레임워크 |
| SQLite3 | 데이터베이스 |
| Axios | HTTP 클라이언트 |
| Anthropic SDK | Claude API |

### 프론트엔드
| 기술 | 용도 |
|------|------|
| React 18 | UI 프레임워크 |
| React Router | 라우팅 |
| Axios | API 호출 |
| CSS 3 | 스타일링 |

## 🔄 데이터 흐름

```
사용자 (브라우저)
    ↓
프론트엔드 (React)
    ↓ (Axios HTTP)
백엔드 (Express)
    ├─→ 데이터베이스 (SQLite)
    └─→ Claude API (평가/분석)
    ↓
응답 (JSON)
    ↓
프론트엔드 (UI 업데이트)
```

## 📊 페이지별 기능

### 홈 (Dashboard)
- 시스템 소개
- 통계 (학생 수, 산출물 수, 평가 수)

### 학생 관리 (StudentManagement)
- 학생 등록/조회/삭제
- 학생 목록 표시

### 산출물 입력 (ArtifactInput)
- 학생별 산출물 입력
- 실천 활동 타입 선택
- 날짜, 차시 기록

### 개별 평가 (IndividualEvaluation)
- 산출물 선택
- AI 평가 수행
- 점수 및 피드백 표시

### 성장 분석 (GrowthAnalysis)
- 학생 선택
- 실천별 여러 산출물 비교
- 성장 과정 분석

### 포트폴리오 (Portfolio)
- 학생의 전체 작업물 분석
- 종합 피드백 생성

### 학급 현황 (ClassStatus)
- 전체 학급 통계
- 평가 점수 분포
- 최근 평가 목록

## 🔐 보안 고려사항

1. **API 키 관리**
   - `.env` 파일에서 관리
   - `.gitignore`에 포함

2. **데이터 비식별화**
   - API 호출 시 학생 이름 제거
   - 로컬 데이터만 저장

3. **CORS 설정**
   - 백엔드에서 프론트엔드 도메인 허용

## 📦 의존성 관리

### 백엔드 (Node.js)
```json
{
  "express": "^4.18.2",
  "sqlite3": "^5.1.6",
  "axios": "^1.6.2",
  "dotenv": "^16.3.1"
}
```

### 프론트엔드 (React)
```json
{
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "react-router-dom": "^6.20.0",
  "axios": "^1.6.2"
}
```

## 🚀 배포 준비

### 프로덕션 빌드

```bash
# 프론트엔드
cd frontend
npm run build  # build/ 디렉토리 생성

# 백엔드
cd backend
# NODE_ENV=production으로 설정
```

## 📚 참고

- **교육 문서**: `ai_assessment_system_guide_v2.md`
- **기술 문서**: `tech_spec_for_developers.md`
- **설치 가이드**: `SETUP.md`

---

**마지막 업데이트**: 2026년 2월 14일
**버전**: 1.0.0
**상태**: 개발 완료 ✅
